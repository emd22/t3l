$macro REALMODE 1
$flag REALMODE

$macro VOID 0
$macro FUNC 1
$macro BYTE 2
$macro WORD 3

boot(BYTE) {
    //pget(fut)
    //rem(fut)
    //halt()
}

decl(fut, BYTE)
set(fut, 5)
stor(fut)

boot(fut)
